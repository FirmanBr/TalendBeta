pipeline {
    agent {
        // Menggunakan agent 'ubuntu'
        label 'ubuntu'
    }
    
    stages {
        stage('Checkout Repository') {
            steps {
                checkout scm
            }
        }
        
    }
}
